# 修复说明 · 线下「停止生成」键失效

对应缺陷：**线下功能的停止生成键失效了**（点了没反应，或者弹了「已停止生成」但内容还在继续往外蹦）。

---

## 一、先回答两个问题

### 这个缺陷是本次改出来的吗？—— 不是

用 `v7.7` 包与当前文件逐字节对比：

```
js1/miya-appointment-engine.js   差异 0 行
js1/miya-appointment-app.js      差异 0 行
```

也就是说，上一次「删楼层回收记忆表」的修复**一行都没碰这两个文件**。
这是个**早就存在的隐性缺陷**，只是这次刚好被撞出来。

### 缺陷长什么样？

链路本身是通的，**断在起点**：

```
点击停止键 → stopOfflineGeneration()
           → eng.stopAppointment(chatId, sessionId)
           → genLife.stop('offline:'+chatId+'::'+sessionId)
           → controllers[key].abort()        ← 只有这里才真的中断 fetch
```

`abort()` **只对已登记的** controller 生效。而登记只发生在 `genLife.begin()`，
它原本只有两个调用点：

| 函数 | 是否登记 controller | 界面是否走它 |
|------|-------------------|-------------|
| `sendAppointment()` | ✅ 有 `begin` | ❌ 不走 |
| `regenerateAppointment()` | ✅ 有 `begin` | ❌ 不走 |
| **`runAppointmentCompletion()`** | ❌ **没有 `begin`** | ✅ **三条路径全走它** |

线下界面的三条生成路径**全都绕开前两个、直连第三个**：

| 界面入口 | 代码位置 | 实际调用 |
|---------|---------|---------|
| 发消息 | `app.js:3559` | `eng.runAppointmentCompletion(...)` |
| 刷新楼层 | `app.js:3903` | `eng.runAppointmentCompletion(...)` |
| 重发 | `app.js:4572` | `eng.runAppointmentCompletion(...)` |

于是 `controllers['offline:...']` **根本不存在** → `stop()` 里那句
`if (ctl) ctl.abort()` 直接跳过 → **fetch 从头到尾没拿到任何 signal**。

雪上加霜的是 `stopAppointment` 当时**无脑 `return true`**，界面据此弹「已停止生成」——
所以你看到的是「提示说停了，内容还在往外蹦」。

---

## 二、修复内容

| 文件 | 放置位置 | 作用 |
|------|---------|------|
| `miya-appointment-engine.js` | `js1/` | 生命周期登记上移到唯一汇聚点 + 幂等复用 + 诚实返回值 |
| `miya-appointment-app.js` | `js1/` | 停止处理改用引擎的真实返回值，不再谎报成功；补测试后门 |
| `index.html` | 根 | 缓存串递增（见下） |
| `audit_offline_stop_generation.py` | `test/` | 新增：单元级 20 项（S0–S7） |
| `e2e_offline_stop_generation.py` | `test/` | 新增：真实浏览器端到端 11 项（G0–G7） |

### 三个关键设计

**1. 登记上移到唯一汇聚点**

把 `genLife.begin()` 从「两个窄入口」移到 `runAppointmentCompletion`。
它是三条路径的**唯一必经之处**，界面怎么调都一定能被停掉。

**2. 幂等 —— 否则会「发消息秒失败」**

`lifecycle.begin()` 内部第一句就是 `stop(key, {reason:'supersede'})`。
如果无条件再 `begin` 一次，会把外层刚建好的 controller **当场 abort**，
`signal` 立刻变 `aborted`，`fetch` 一发起就死。

所以：已有 controller 就**复用**，没有才登记。用 `owner` 标记区分
`'outer'`（外层持有，收尾归外层）/ `'self'`（本函数持有，收尾归本函数）/
`'none'`（无生命周期模块，退化为不可停止但不报错）。

**3. 诚实返回值 —— 不再谎报成功**

`stopAppointment` 改为返回「有没有真的 abort 到在跑的生成」：

- 有 controller → `true`，界面弹「已停止生成」
- 没有 controller（本来就没在跑）→ `false`，界面**静默**

这样「明明没停却说停了」的错觉从根上消失。

### 顺带修掉的一个隐患

abort 之后 `replyInFlight` 忙碌标记可能来不及清（外层 `finally` 不一定还有机会跑），
而只要它还在，用户下一句就会被 `busy` 挡回去 —— 表现为**停止后发不出消息**。
现在由 `runAppointmentCompletion` 自己在 reject 分支里确保清掉。

---

## 三、缓存串已更新

`index.html` 里这两处 `?v=` 已递增：

| 资源 | 旧 | 新 |
|------|----|----|
| `js1/miya-appointment-engine.js` | `v=126` | **`v=127`** |
| `js1/miya-appointment-app.js` | `v=141` | **`v=142`** |

> 本项目 `sw.js` 已把缓存 key **归一化**（去掉查询串），静态资源走
> `networkFirst` + `cache:'no-cache'`，即使漏 bump 也不会喂旧代码；
> 但按既有习惯照常递增更稳妥。

---

## 四、验证结果

### A/B 对照（回滚修复 → 缺陷复现）

| 测试 | 回滚后 | 修复后 |
|------|-------|-------|
| 单元（audit） | 11/16 ❌ | **20/20 ✅** |
| 端到端（e2e） | 7/11 ❌ | **11/11 ✅** |

回滚后失败项正是用户看到的症状：

```
✗ G2 直连 runAppointmentCompletion 后 controller 就位
     实际：False
✗ G3 stopAppointment 真的 abort 了 signal
     实际：None
✗ G4 停止传导到网络层：fetch 收到 signal
     实际：False
```

### 单元测试覆盖（20 项）

| 编号 | 覆盖点 |
|------|-------|
| S0–S2 | 生命周期机制本身可用（begin → stop → abort） |
| S3 | **缺陷本体**：未 begin 时 stop 是空转，却返回 true |
| S4 | 静态校验：汇聚点正确 + 登记在所有 await 之前 + 幂等就位 |
| S5–S6 | 引擎真实加载后可停 |
| S6d | **核心回归**：界面直连路径 controller 就位且能 abort |
| S6e | 幂等：外层已登记时 controller 被复用、不被顶掉 |
| S6f | **诚实返回值**：没在跑 → false；在跑 → true 且真的 abort |

### 端到端覆盖（11 项，真实浏览器）

| 编号 | 覆盖点 |
|------|-------|
| G0 | API 配置就绪 + 真实会话搭建（前置） |
| G1 | 初始为发送态（未误挂 `is-stop`） |
| G2 | 直连路径 controller 就位 |
| G3 | `stopAppointment` 真的 abort |
| G4 | **停止传导到网络层**：fetch 收到 signal 且被中止 |
| G5 | 停止后忙碌标记被清理（不会「停止后发不出消息」） |
| G6 | 幂等：controller 引用复用、未被误 abort |
| G7 | UI 收尾：`is-stop` 撤掉、输入框解锁 |

### 回归测试（无退化）

| 测试 | 结果 |
|------|------|
| injection_unit | ✅ 42/42 |
| injection_integration | ✅ 16/16 |
| worldbook_regression | ✅ 43/43 |
| sw_cache_regression | ✅ |
| audit_memory_table_floor_delete | ✅ 26/26 |
| e2e_memory_table_floor_delete | ✅ 6/6 |
| e2e_offline_memory_table_floor_delete | ✅ 5/5 |
| e2e_mirror_leak_after_delete | ✅ 6/6 |

---

## 五、覆盖方式

**直接解压整包覆盖即可** —— 包内是完整项目，本次修复与上次的记忆表修复
**均已应用在位**，不需要分两次打补丁。

若只想拿最小改动集，本目录下这三个文件对应放置即可：

- `miya-appointment-engine.js` → `js1/`
- `miya-appointment-app.js` → `js1/`
- `index.html` → 根目录

---

## 六、跑测试

```bash
# 在项目根目录起服务（两个端口都要，部分用例指定 8098）
python3 -m http.server 8099 &
python3 -m http.server 8098 &

# 单元级（不需要浏览器）
python3 test/audit_offline_stop_generation.py

# 端到端（需要浏览器 + 上面的服务）
python3 test/e2e_offline_stop_generation.py
```

---

## 七、另一个顺带发现（未改，供你判断）

`js1/miya-appointment-app.js` 里 `stopOfflineGeneration()` 的 UI 收尾是
**无条件执行**的（撤 `is-stop`、解锁输入框、清 `xw-generating`），
即使引擎侧什么都没停。

这个行为我**保留原样**了，理由是：宁可让用户能继续操作，也不要因为
一次异常把输入框永久锁死。现在引擎返回值已经诚实，界面只在**真停住**时才提示，
两边配合起来不会再误导。

如果你希望 UI 也改成「只有真停住才复位界面」，告诉我，改动很小。
