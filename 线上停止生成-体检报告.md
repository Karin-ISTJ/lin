# 线上「停止生成」链路体检报告

> 触发点：用户指出「线上的停止生成是和线下一起加的」。
> 同一批加进来的实现，必须按同样的怀疑力度查一遍。

---

## 一、结论

| 项目 | 结果 |
|------|------|
| 线上停止链路**主功能** | ✅ **是好的**，四环齐备 |
| 发现的问题 | 1 处**重复提示** + 1 处**同族谎言**（已修） |
| 与线下的关键差异 | 线下**缺第一环**（没人 `begin`）；线上**四环完整** |

**线上没有线下那个致命缺陷。**但它有另外两个问题，其中一个已经存在很久。

---

## 二、四环可达性验证（核心）

线下失效的直接原因是「控制器从未登记」，即四环里缺了第一环。
逐环核对线上：

| 环 | 内容 | 线上状态 | 证据 |
|----|------|---------|------|
| ① 登记 | `genLife.begin('chat:'+chatId)` | ✅ 有 | `miya-chat-engine.js:4238` |
| ② 取 signal | `genSignal = genCtl.signal` | ✅ 有 | `:4240` |
| ③ 传参 | `fetchChatCompletion(..., genSignal)` | ✅ 有 | `:4407` |
| ④ 写入 fetch | `if (signal) opts.signal = signal` | ✅ 有 | `:3702` |

**四环全部存在且在同一条调用链上** —— 这是线上能正常停止的原因。

> 对比线下：① 缺失，②③④ 都在，但 ② 拿到的是 `undefined`，
> ③④ 于是认真转发了一个**不存在的东西**。

### 生成入口唯一性核查

线上能绕过 `begin` 风险的关键前提：**只有一个生成入口**。

```
fetchChatCompletion 的调用点：
  :3721  内部重试递归（同一个 signal 继续传）
  :4407  sendChat 内（genSignal 来源）
```

已核实全项目仅此两处，且 `:3721` 是 `:4407` 的下游。**没有旁路入口** ——
这与线下「三条路径全绕开 begin」形成鲜明对比。

---

## 三、发现的问题

### 3.1 【已修】重复提示：点一次停止，弹两次

```js
// 发送键点击处理（miya-chat-room.js）
if (stopped) toast('已停止生成');     // ← 第一次

// sendChat 的 catch（同文件）
else if (code === 'aborted' || ...) {
  toast('已停止生成');                // ← 第二次
}
```

用户点一次停止，会看到**两条**相同提示。

### 3.2 【已修】catch 侧的 abort 提示语义是错的

比重复更严重：`catch` 侧的 abort **不一定来自用户**。

`abort` 有三种来源：

| 来源 | 触发方 | 该不该提示「已停止生成」 |
|------|-------|---------------------|
| 用户点停止键 | `stopChatGeneration` | ✅ 应该 |
| 新一轮生成顶掉上一轮 | `lifecycle.begin` 内的 `supersede` | ❌ **不该** —— 用户没点 |
| 清空聊天记录 | `lifecycle.stop(reason:'clear')` | ❌ **不该** —— 用户没点 |

旧代码在 `catch` 里一律弹「已停止生成」，所以**开新对话顶掉旧生成时，
用户会莫名收到「已停止生成」**。这是长期存在的误报。

### 修复：提示所有权唯一

判给**点击处理**，因为只有它知道用户意图（它持有 `stopped`，即真的中断到了
生成）；`catch` 只负责**安静地收尾**。

> 这正是线下 `runStream` 的既有做法 —— 它的 `catch` 里
> `if (isAbortError(err)) return;` 直接静默。两边现在语义一致了。

### 3.3 【已修】`stopChatGeneration` 无脑 `return true`

与线下 `stopAppointment` 完全同款：无论有没有停到东西都返回 `true`。

线上**当前不会**因此出问题（因为四环齐备，每次都真能停到），
但这个谎言本身就是**未来故障的种子** —— 一旦有人再加一条绕过 `sendChat`
的生成路径，就会**原样重演**线下那个 bug。所以一并收口为 `return !!ctl`。

---

## 四、与线下的差异对照

| | 线下 | 线上 |
|---|------|------|
| 生成入口 | **3 条**，全绕开 `begin` | **1 条**，正确 `begin` |
| ① 登记 | ❌ 缺失 | ✅ 有 |
| 停止是否真的生效 | ❌ 空转 | ✅ 生效 |
| 返回值 | 谎报 `true` | 谎报 `true` |
| abort 提示 | 点击处理弹（catch 静默） | **两边都弹** |
| 停止后忙碌标记 | 曾可能漏清 | ✅ `releaseChatApi` |

**两个"谎报 true"是同一批加的** —— 用户的记忆是准确的。

---

## 五、验证

### 新增测试 `test/audit_online_stop_generation.py`（17 项）

| 编号 | 覆盖点 |
|------|-------|
| O1 | `sendChat` 登记 controller + 取出 signal |
| O2 | signal 写入 fetch init + 调用处透传 |
| O4 | 备用 API 回退路径保留同一 signal（不中途换/丢） |
| O5 | 重试递归继续传 signal |
| O6 | `stopChatGeneration` 返回值语义 |
| O7 | 停止后释放忙碌标记 |
| O8 | 界面侧用真实返回值、只在真停住时提示 |
| **O9** | **提示所有权唯一**（去注释后「已停止生成」仅 1 处 + catch 静默） |
| R0–R2 | Node `vm` 加载真实源文件，运行时验证四环齐备 |

### A/B 对照（注入线上缺陷家族）

```
❌ O6a stopChatGeneration 不再无脑 return true
❌ O9a 去注释后「已停止生成」提示只有一处
❌ O9b sendChat 的 catch 对 abort 保持静默
通过 14/17   ← 注入后
通过 17/17   ← 修复后
```

### 回归

单元级 6 套 + 浏览器级 8 套全绿（含 `regression` 13/13、
`injection_unit` 42/42、`worldbook_regression` 43/43、
记忆表 26/26、线下 e2e 5/5、线上 e2e 6/6、配平扫描 HIGH 0）。

---

## 六、修复文件

| 文件 | 放置位置 | 说明 |
|------|---------|------|
| `miya-chat-engine.js` | `js1/` | `stopChatGeneration` 诚实返回值（`v=50→51`） |
| `miya-chat-room.js` | `js1/` | catch 侧静默 + 点击侧按真实结果提示（`v=62→63`） |
| `audit_online_stop_generation.py` | `test/` | 新增体检 17 项 |

---

## 七、一个值得记住的差异

线下和线上是**同一批**写的停止功能，结局却不同，差别只在**一个环节**：

> 线下有 3 个生成入口，`begin` 加在了其中 2 个上；
> 线上只有 1 个入口，`begin` 恰好就在那 1 个上。

**所以线下是坏的，线上是好的 —— 纯属巧合，不是设计。**

这提示一件更普遍的事：**「登记类」调用不该分散在各个入口**，
而要收敛到**所有路径的必经之处**。线下修复时我做的正是这件事
（把 `begin` 上移到 `runAppointmentCompletion`）。

这也解释了为什么当初「一起扫都没发现问题」：
按文件逐个读，线上那份**读起来挑不出毛病** —— 它确实没毛病。
问题是这种「正确」依赖于「入口恰好只有一条」这个**隐性前提**，
而代码里没有任何东西保证这个前提。
