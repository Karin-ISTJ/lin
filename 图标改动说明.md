# 图标改动说明

## 一、替换「设置」图标

新图标包（`icon_79wlsd3gtv4.zip`）里**没有齿轮形状的图标**，12 个文件分别是：

| 文件 | 实际形状 |
|---|---|
| `a-ThemeDark.svg` | 月亮 |
| `a-TwitterPublish.svg` | 树叶 |
| `check-rec.svg` | 圆角方框 |
| `coin.svg` | 金币 |
| `community.svg` | 人物群 |
| `copy.svg` | 双页复制 |
| `Switch.svg` | 双箭头 |
| `kgunbangchicangzhanbi.svg` | 四菱形图案 |
| `star-ai.svg` | 闪光（四角星） |
| `star.svg` | 五角星 |
| `TG.svg` | 纸飞机 |
| `World_Source.svg` | 地球 |

按你的指定，用 **`coin.svg`（金币）** 作为新的「设置」图标。

**改动位置：** `js1/app.js` → `SVG_CLASSIC.set`

- 旧：24×24 线性齿轮（`stroke` 描边，圆环 + 齿轮外框）
- 新：1024×1024 `fill: currentColor` 实心金币图形

> 注意：`js1/miya-deep-app.js` 里也有一份 `ICONS.settings`（角色手机桌面 + Dock 用），**本次未改动** —— 因为角色手机那套文件没有被 `index.html` 引用，暂时打不开（详见第四节）。

## 二、桌面图标统一 26px

原先桌面图标 ssvg 尺寸散落在 22px / 24px 两档，现已全部统一为 **26px**：

| 文件 | 选择器 | 原值 | 新值 |
|---|---|---|---|
| `css/style.css` | `.ic__box svg` | 22px | **26px** |
| `css/miya-desk-custom.css` | `.desk-custom__grid .desk-custom__ic .ic__box svg` | 24px | **26px** |
| `css/miya-desk-custom.css` | `.foot__dock--custom .desk-custom__dock-ic .foot__dock-btn svg` | 24px | **26px** |
| `css/miya-apps.css` | `.ins-icon-pick__glyph`（美化面板图标选择器） | 22px | **26px** |

图标外框（`.ic__box` / `.foot__dock-btn`）尺寸不变，仍是 58px / 52px。仅图标本身从 22/24px 放大到 26px。

### 白名单补全

`css/miya-desk-custom.css` 里有一组「实心图标专用」规则（去掉玻璃白底、强制 26px），原先只覆盖 `beauty / imagegen / chat / book / stpreset / notes / contacts / memory / itinerary / couple / fun` 共 11 个 App。

`set`（设置）**不在**这个名单里，而它正是 `CUSTOM_GRID_APPS` 的第一个图标。现已把 `[data-app="set"]` 补进全部 **5 处**白名单，让新设置图标和其他图标享受同样的去白底 + 26px 处理。

## 三、浏览器缓存版本号

为避免旧缓存，已同步抬高版本号：

| 文件 | 旧 | 新 |
|---|---|---|
| `index.html` → `css/style.css` | v=40 | **v=41** |
| `index.html` → `css/miya-apps.css` | v=30 | **v=31** |
| `index.html` → `css/miya-desk-custom.css` | v=48 | **v=49** |
| `index.html` → `js1/app.js` | v=79 | **v=80** |
| `sw.js` → `CACHE` | `miya-v114-karin` | **`miya-v115-karin`** |

## 四、需要注意的两个既有问题（本次未改）

### 1. 角色手机（Char Phone）整套打不开

`js1/miya-deep-*.js`、`js2/miya-deep-*.js`、`css/miya-deep-*.css` 共 70+ 个文件都在项目里，但：

- `index.html` 里**没有任何一处**引用这些资源（`miya-deep` 出现次数为 0）
- `js2/miya-lazy-boot.js` 的懒加载分组里**也没有** `deep` 分组
- `#miya-deep-app`、`#dp-phone-root` 这两个根节点在 DOM 里**不存在**

也就是说角色手机桌面目前压根打不开 —— 这是原始压缩包里就有的状态，不是本次改动造成的。按你的决定，本次只处理主线桌面。

### 2. 图标外框尺寸不统一（既有差异）

- 网格图标 `.ic__box` = **58px**
- Dock 图标 `.foot__dock-btn` = **52px**

图标本身已统一 26px，但外框仍是 58 / 52 两档。这属于原有设计，未做改动。

## 五、验证结果

用 Chromium 实测渲染后，12 个桌面图标的实际尺寸：

```
set:26  book:26  memory:26  chat:26  beauty:26  stpreset:26
couple:26  itinerary:26  notes:26  fun:26  imagegen:26  contacts:26
```

全部为 **26×26**，`viewBox` 均为 `0 0 1024 1024`，`fill` 均为 `currentColor`，风格一致。
