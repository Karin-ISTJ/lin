# 结构性缺陷家族扫描报告

> 起因：线下「停止生成」键失效，且用户反馈「以前是好的」。
> 定位后发现根因是一类**结构性**缺陷，而非某一行写错。
> 本报告对全项目做同类缺陷的系统性扫描。

---

## 一、这类缺陷为什么难发现

停止键的根因有四个特征，缺一个都不会这么难查：

| 特征 | 后果 |
|------|------|
| 调用方存在、被调用方存在、语法合法 | 静态阅读**每一环都打勾** |
| 缺的是「让机制生效的那次登记」 | 缺失的代码**不在文件里**，grep 找不到 |
| 不报错，静默跳过 | 日志干净，无从下手 |
| `return true` 谎报成功 | 调用方以为成了，错误被吞掉 |

最阴的是最后一条：**报错会给你线索，谎报只会让你怀疑自己的操作。**

核心矛盾在于：

> 静态阅读发现不了「不存在的代码」。

所以扫描不能靠「读」，必须做**「读侧 vs 写侧」的交叉可达性验证**。

---

## 二、扫描方法

新增 `test/audit_pairing_integrity.py`，针对六类成对调用模式：

| 编号 | 模式 | 缺陷形态 |
|------|------|---------|
| **P1** | 生命周期 scope 配平 | 有 `stop(scope)` / `getController(scope)`，无 `begin(scope)` |
| **P2** | controller/signal 读写配平 | 读侧存在，写侧不可达 |
| **P3** | signal 转发链完整性 | 认真转发 `handlers.signal`，但该字段从未被赋值 |
| **P4** | `return true` 型谎报 | stop 类函数不判断「有没有真的停到东西」 |
| **P5** | 事件监听配平 | `addEventListener` 无对应 `removeEventListener` |
| **P6** | 定时器配平 | `setInterval` 无 `clearInterval` 且无幂等守卫 |

实现要点：

- **注释剥除保持行号**：用等长空白替换，否则报出的位置全是假的
- **函数体按大括号配平提取**，而非正则贪婪匹配
- **scope 表达式归一**：写侧 `'chat:' + String(chatId)` 与读侧 `'chat:' + id`
  变量名不同、取值相同，必须归一成「形状键」+「字面前缀」两级匹配

> 这一条是踩坑换来的：第一版按字面量比对，把线上**正确**的
> `stopChatGeneration` 误报成 HIGH「有读无写」。

---

## 三、扫描结果

```
🟡 HIGH    0 项
🟡 MEDIUM  0 项
⚪ INFO   24 项
```

**修复前**（同一扫描器）：

```
🔴 HIGH    4 项  ← 含 2 项谎报成功 + 2 项误报
🟡 MEDIUM  2 项  ← 均为误报
```

### 3.1 修复的问题

| 位置 | 问题 | 处理 |
|------|------|------|
| `js1/miya-chat-engine.js:4197` `stopChatGeneration` | 无脑 `return true`，与线下 `stopAppointment` 同款谎言 | 改为 `return !!ctl` |
| `js1/miya-chat-room.js:5482` 线上停止键 | 无论成败一律弹「已停止生成」 | 改用引擎真实返回值 |
| `js2/miya-generation-lifecycle.js:80` `stop` | 底层就不区分「有没有停到东西」 | 改为 `return !!ctl` |

> **线上当前是好的** —— `sendChat` 会 `begin`，且它是唯一的生成入口
> （已核实：全项目只有 `sendChat` 调用 `fetchChatCompletion`）。
> 但这个谎言不该留着：一旦将来有人再加一条绕过 `sendChat` 的生成路径，
> 就会**原样重演**线下那个 bug。所以一并收口。

### 3.2 排查后确认的误报

| 位置 | 初判 | 实际 |
|------|------|------|
| `miya-chat-engine.js:4201` scope 有读无写 | HIGH | **误报**：写侧 `'chat:'+String(chatId)` 与读侧 `'chat:'+id` 变量名不同、同源 |
| `miya-chat-room.js:5485` scope 有读无写 | HIGH | **误报**：同上，前缀 `chat:` 已配平 |
| `miya-lockscreen.js:476` `setInterval` 无 clear | MEDIUM | **误报**：`init()` 内时钟心跳，文件尾只调用一次（`DOMContentLoaded`） |
| `miya-weather-bridge.js:1239` `setInterval` 无 clear | MEDIUM | **误报**：`if (careScanTimer) return;` 幂等守卫 + 走 `miyaBgSetInterval` |

针对这两类误报，扫描器已加入识别逻辑：

- **scope 同源判定**：形状键不同则退一步按字面前缀配平
- **页面级单例心跳识别**：幂等守卫 / 生命周期封装 / 初始化作用域，命中任一即视为安全

### 3.3 保留为 INFO 的项

| 类型 | 数量 | 说明 |
|------|------|------|
| P5 面板类文件无 `removeEventListener` | 22 | 长生命周期节点（面板 DOM 常驻），通常无害 |
| P6 单例心跳 | 2 | 已识别为安全形态 |

> INFO 不是「没问题」，而是「静态扫描不足以判定，需人工确认」。
> 判据：`addEventListener` 若绑在**会反复重建的 DOM** 上才会泄漏。
> 本次抽检的 22 处均为常驻面板节点，维持原状。

---

## 四、扫描器自身的 A/B 验证

一个只报「全绿」的扫描器没有价值 —— 它可能什么都没在扫。

**验证方法**：把原始缺陷重新注入干净代码树，看扫描器能否抓回：

```bash
cp <原始版本>/js1/miya-appointment-engine.js  js1/     # 回滚修复
sed -i 's|return !!ctl;|return true;|'        js1/...  # 恢复谎言
sed -i 's|return !!ctl;|return true;|'        js2/miya-generation-lifecycle.js
python3 test/audit_pairing_integrity.py
```

**结果**：

```
🔴 HIGH（3 项）
  [P1] js1/miya-appointment-app.js:3035
        有读无写：该 scope 被 stop/getController 读取，但找不到对应 begin
        scope=V  前缀=(无)  读侧 6 处
  [P4] js1/miya-appointment-engine.js:2492
        stop 类函数无脑 return true（可能谎报成功）
        函数 stopAppointment 未基于「是否真的停到东西」判断返回值
  [P4] js2/miya-generation-lifecycle.js:97
        stop 类函数无脑 return true（可能谎报成功）
        函数 stop 未基于「是否真的停到东西」判断返回值
```

✅ 扫描器**精确抓回了原始缺陷** —— 包括 P1 那条「离线路径缺 `begin`」，
正是停止键失效的直接原因。

**修复版扫描退出码 = 0**（仅 HIGH 时非零，可直接接进 CI）。

---

## 五、一个方法论上的教训

我在前一次「扫线下功能不足」时**没能发现这个 bug**。复盘原因：

| 上次做法 | 这次做法 |
|---------|---------|
| 读调用链 | **Node `vm` 加载真实源文件跑起来** |
| 判据：「每一环看起来都对」 | 判据：**`getController(scope)` 是不是 `null`** |
| 只能看「有什么」 | **能发现「该有的没有」** |
| 无法证伪 | **A/B：回滚 → 变红** |

关键一步是**先写测试、看它失败**。写 `S6d1`（直连后 controller 是否就位）
时预期它会过 —— 结果它是红的。那一刻 bug 才真正暴露。

具体失误：

1. **没有为每条「用户可操作路径」建立覆盖清单** —— 停止键是明确的 UI 按钮，
   居然没有一条测试碰它
2. **没有对「跨模块的成对调用」做交叉验证** —— `stop(scope)` 和 `begin(scope)`
   必须成对，我只看了 `stop` 侧，没核对 `begin` 侧有没有配平
3. **对「转发型代码」过于信任** —— 看到 `signal: handlers.signal` 就认为链路通了，
   没去追这个 `handlers.signal` 是谁写的

最根本的：**我用「读」来代替「跑」。**

---

## 六、扫描器用法

```bash
# 在项目根目录
python3 test/audit_pairing_integrity.py

# 退出码：0 = 无 HIGH；1 = 存在 HIGH（可接 CI）
```

新增文件：

| 文件 | 说明 |
|------|------|
| `test/audit_pairing_integrity.py` | 配平扫描器（P1–P6） |

修复文件：

| 文件 | 放置位置 | 说明 |
|------|---------|------|
| `miya-chat-engine.js` | `js1/` | `stopChatGeneration` 诚实返回值 |
| `miya-chat-room.js` | `js1/` | 线上停止键不再谎报成功 |
| `miya-generation-lifecycle.js` | `js2/` | `stop` 如实报告是否停到东西 |

---

## 七、局限（说在前面）

这套扫描**不能替代**运行时测试，原因：

1. **动态注册抓不到** —— 通过字符串拼接的 scope、运行时构造的函数名，
   静态分析看不见
2. **语义正确性判不了** —— 它只能判断「读写配平」，判断不了「逻辑对不对」
3. **INFO 档需要人看** —— 22 条 P5 是抽检确认的，不是全量证明

它的价值在于**结构性地捞出整个 bug 家族**，把「靠人仔细看」
变成「靠机器扫一遍」。停止键只是露出了水面的一角 ——
这类「半截活」的迁移痕迹，现在有了一个固定的检查手段。
